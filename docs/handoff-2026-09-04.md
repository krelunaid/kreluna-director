# Ripresa lavoro — 4 settembre 2026

## Stato verificato

- OAuth Gmail locale collegato: rinnovo verificato anche in un nuovo processo,
  senza un secondo consenso. Configurazione e token sono fuori da Git; token
  cifrati nel database locale. Il progetto Google resta in modalità Test.
- Implementato recupero del codice per la sola validazione postazione Webdesk:
  opt-in del titolare, richiesta firmata dell’Agent assegnato, scadenza breve,
  mittente/DMARC/destinatario/login verificati, consegna monouso, niente codice
  in log, screenshot o database.
- La prova reale isolata ha raggiunto la conferma di postazione validata.
  Il successivo link «Accedi a webdesk» è stato aggiunto e provato separatamente:
  ha aperto `/Apps/Dashboard/View`. Non è ancora una prova completa delle fatture.
- Corretto il riconoscimento delle finestre Safari senza schede e il controllo
  dell’esito login: la scomparsa del campo password da sola non è successo.
- Aggiornati localmente Kreluna Director.app e Kreluna Agent.app, con backup
  delle app precedenti e verifica delle firme locali. Nessun nuovo rilascio
  pubblico/versione è stato pubblicato. I processi non sono stati riavviati:
  quelli già in esecuzione potrebbero usare ancora il codice precedente.
- Autorizzazione locale ai codici Webdesk attivata su richiesta del titolare.
  Nessuna vecchia richiesta in coda è stata avviata durante questi controlli.

## Da fare

1. Controllare stato dei processi e coda prima di un riavvio; non eseguire
   automaticamente vecchie richieste demo o fatture in attesa.
2. Ripetere il percorso intero con le app aggiornate, inclusa la continuazione
   dopo la validazione. Gli script di prova sono descritti in gmail-connector.md.
3. Provare la compilazione della fattura reale con dati confermati, fino alla
   sola visualizzazione. Verificare più righe, IVA e dichiarazione d’intento.
4. Il browser indipendente «Browser Kreluna» è stato solo discusso: NON è
   implementato. Attualmente l’Agent controlla ancora il browser del Mac.
   L’obiettivo è permettere all’utente di lavorare senza interferire con mouse,
   scheda attiva o finestra dell’Agent; un secondo cursore grafico non basta.
5. Prima della distribuzione ai clienti completare configurazione Google di
   produzione e requisiti di verifica; non promettere consenso permanente in Test.

## Vincoli permanenti

Non salvare bozze/fatture nel gestionale, non emettere e non inviare fatture
automaticamente. Non includere credenziali, PIN, token, database, screenshot o
artefatti di build nei commit. Gli altri OTP, SPID/CNS e pagamenti sono esclusi
dall’eccezione Webdesk. Mai presentare una prova di accesso come prova di fattura.
